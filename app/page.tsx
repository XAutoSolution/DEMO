// Intentionally blank landing page: no public index of client demos.
// Each demo lives at its own slug (e.g. /relevo-market, /relevo-market-s).
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-neutral-50 p-8">
      <p className="text-sm text-neutral-300">—</p>
    </main>
  );
}
